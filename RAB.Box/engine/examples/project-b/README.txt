EXAMPLE PROJECT ONLY
The PATHS.json adapter layout is proposed in this package. Replace bindings with your real house data.
Project A uses keyed options; Project B demonstrates the equivalent array form and extra required fields.
settings.js is included as an opt-in alternative; default discovery reads JSON only.
