{
  "id": 1830000000015,
  "name": "folders",
  "title": "Index project folders",
  "description": "Writes a bounded local folder-ID index with parent IDs and coverage; audited input remains untouched.",
  "settings": [
    {
      "name": "project_id",
      "type": "number",
      "title": "Project ID",
      "required": false,
      "description": "Uses the selected project when omitted."
    },
    {
      "name": "folder",
      "type": "folder",
      "title": "Folder",
      "required": true
    },
    {
      "name": "max_duration_ms",
      "type": "number",
      "title": "Time limit (ms)",
      "required": false,
      "default": 300000
    }
  ],
  "meta": {
    "domain": "audit",
    "operation": "index",
    "target_type": "folder",
    "authority": "read"
  }
}
