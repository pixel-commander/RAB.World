Completed audit results are saved here as JSON, grouped by session.
Each report includes the Tool, input options, result, and execution trace.
The Tool runner uses tool-runs when no session is supplied.
