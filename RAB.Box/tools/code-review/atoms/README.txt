Review Code Atoms
Uses the parent's executor with meta.group=atoms. Calls existing css-tokens and
css-states auditors through the shared scratch runner helper. CSS only. These
checks cover token use and duplicate states, not every OOCSS architectural rule.
See ../README.txt for examples, storage and limitations.
