REFERENCE MATERIAL — NOT RUNTIME CONFIGURATION
=============================================
These are verbatim copies of earlier companion downloads, included so the
word lists, numbered concepts, names/dates, and math notes can travel with source.

rraabbiitt_formal_neighborhood_and_rediscovery_map.txt
    The earlier 22-item map and reconstructed people/theories/dates list.
    Its original provenance notes remain. Historical claims were not re-researched
    during this packaging-only pass.

lexicon-v0.1/word-map.txt and lexicon-v0.1/lexicon.json
    Earlier typed-lexicon seed and readable word map. Some entries are explicitly
    RESERVED, not implemented. These files are not loaded by the v0.2 runtime.
    They do not extend the current parser merely by being present here.

For what the current program actually supports, use the README and CONTRACTS.txt
inside magic-box-stamps-v0.2/. Do not treat older design analogies as guarantees.
