CSS atoms live here. Category beacon.json files identify the indexed roots.
Each atom's settings.json is a signal; kind preserves its specific atom type.
Default CSS is loaded through index.css. Shared theme tokens come from the host.
container-machine-trim is a metadata-only placeholder; its CSS is still pending.
