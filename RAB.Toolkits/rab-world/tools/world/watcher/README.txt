WORLD WATCHER
=============

Saves world-watcher.json in PATH with the WORLDS_PATH a host can use to run
World Scanner. A host-managed watcher observes only registered beacon.json
records and triggers a scanner pass when one is added, changed, or removed.
This tool deliberately does not start a background process or open a port.
