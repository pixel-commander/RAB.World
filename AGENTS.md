# RAB.World project orientation

The working project root is `C:\RAB.World` on this computer.

## Cold start

- Read `README.txt` beside this file before project work. It owns the project purpose, folder responsibilities and cold-start sequence; do not maintain a second overview here.
- If the session starts in another directory named RAB.World, use the working root explicitly for reads, commands and edits.
- After reading the overview, load only the instructions, rules, skills and contracts relevant to the current task. Read more specific `AGENTS.md` and `RULES.txt` files before editing their area.
- Keep personal material under `__docs/personal` out of project orientation, boot summaries and project-purpose documentation. Access it only when the user explicitly asks for work there.
- Preserve the existing house boot and world-resolution instructions supplied to the session. This local project is not a replacement for the server-owned house registry.

## Working context

- Use current file owners and configured paths. Server drive letters and old paths in copied documentation are not proof of a local location or a working connection.
- Follow the README's cold-start sequence and the relevant area's contracts. Report missing guidance or unverified behavior plainly.
- The current user request defines the work. Reading a plan, review, example or historical record does not authorize executing the work it describes.
- Once oriented, complete the requested task. If the request is orientation alone, give a short project summary and any material gap.
